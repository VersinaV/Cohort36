package lesson_46.classwork;

public class MathExeple {

  public static void main(String[] args) {
    /*
       Math.abs(x): Возвращает абсолютное значение числа x.

      int absoluteValue = Math.abs(-5); // Возвращает 5
      Math.pow(x, y): Возвращает x в степени y.


      double powerValue = Math.pow(2, 3); // Возвращает 8.0 (2^3)
      Math.sqrt(x): Возвращает квадратный корень из числа x.


      double squareRootValue = Math.sqrt(25); // Возвращает 5.0 (квадратный корень из 25)
      Math.ceil(x) и Math.floor(x): Округляют число x вверх или вниз соответственно до
      ближайшего целого числа.


      double roundedUp = Math.ceil(4.3); // Возвращает 5.0 (округляет 4.3 вверх)
      double roundedDown = Math.floor(4.7); // Возвращает 4.0 (округляет 4.7 вниз)
      Math.max(x, y) и Math.min(x, y): Возвращают наибольшее или наименьшее из двух чисел x и y.


     int maxNumber = Math.max(10, 20); // Возвращает 20 (наибольшее из 10 и 20)
     int minNumber = Math.min(10, 20); // Возвращает 10 (наименьшее из 10 и 20)
     Math.random(): Генерирует случайное число в диапазоне от 0.0(включительно) до 1.0(исключительно).

     double randomValue = Math.random(); // Возвращает случайное число между 0.0 и 1.0
      Это только несколько примеров методов из библиотеки java.lang.Math. Она также содержит
       множество других полезных математических методов для работы с числами и выполнения различных
       математических операций в Java.
     */
  }

}
